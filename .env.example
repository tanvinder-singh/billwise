PORT=3000
JWT_SECRET=change-this-to-a-random-secret-string

# SMTP for email OTP (leave blank → dev mode logs OTP to console)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@billwise.in

# Twilio for phone OTP (leave blank → dev mode logs OTP to console)
TWILIO_SID=
TWILIO_AUTH_TOKEN=
TWILIO_PHONE=
